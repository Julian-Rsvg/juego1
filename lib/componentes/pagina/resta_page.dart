import 'package:flutter/material.dart';

class RestaPage extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text('Resta pagina'),
      ),
    );
  }
}